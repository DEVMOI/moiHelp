# moiHelp
> Helpers Used in DEVMOI Projects

Helper:
```
 // General Storage
 toCapitalize(str)
 copyToClipboard(str)
 parseDate(date,seperator)
 isJson(str)
 isEven(int)
 // Local Storage
 setStorage(key,item)
 getStorage(key)
 removeStorage(key)
 clearStorage(key)
```