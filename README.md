# moihelp
> Helpers Used in DEVMOI Projects

Helper:
```
 // General Helpers
 toCapitalize(str)
 copyToClipboard(str)
 parseDate(date,seperator)
 isJson(str)
 isEven(int)
 // Local Storage
 setStorage(key,item)
 getStorage(key)
 removeStorage(key)
 clearStorage(key)
```